/*
 *Copyright(c) 李文龙
 *All rights reserved.
 *
 * 文件名称：demo.c
 * 文件标识：
 * 摘    要：代码编写风格样例
 *
 * 当前版本：1.0
 * 作    者：李文龙
 * 完成日期：2024年2月6日
 *
 * 取代版本：
 * 原作者  ：
 * 完成日期：
 */

#include <stdio.h>

int main()
{

    return 0;
}

/*
 * 函数介绍：
 * 输入参数：
 * 输出参数：
 * 返回值  ：
 *
 */

void Function(float x, float y, float z)
{
    //...
}

/*
 * 类
 */

/* class A
{
    public:
    void Function(float x, float y, float z)
    ...

    private:
        int i;
        ...
} */

/* BOOL
    if (flag) // 表示 flag 为真
    if (!flag) // 表示 flag 为假
float
    if ((x>=-EPSINON) && (x<=EPSINON)) //其中EPSINON是允许的误差（即精度）。
指针
    if (p == NULL) // p 与 NULL 显式比较，强调p 是指针变量
    if (p != NULL)   */